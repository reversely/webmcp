# Copy to .env, or let `npm run setup` link this app's .env to the shared one at the repo root.

# Optional. 1 loads Chrome's WebMCP polyfill for browsers without document.modelContext.
# The Playwright suite passes ?webmcp=polyfill in the URL instead.
NEXT_PUBLIC_WEBMCP_POLYFILL=
