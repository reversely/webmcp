[project]
name = "webmcp"
version = "0.1.0"
description = "Python side of the room planner: the Modal image-to-3D endpoint and the commit hooks"
requires-python = ">=3.13"
dependencies = [
    "modal>=1.5.4",
]

[dependency-groups]
dev = [
    "detect-secrets>=1.5.0",
    "pre-commit>=4.6.2",
    "ruff>=0.16.5",
]
